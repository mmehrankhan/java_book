
var num=+prompt('Enter a number: ')

    if (num>0) {
       document.write( num+' is  POSITIVE.');
    } else if (num<0) {
       document.write( num+' is  NEGATIVE.');
    } else if (num==0) {
        document.write(num+' is  nutral.');
    } else {
       document.write( 'Invalid input !');
    }

